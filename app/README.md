# App - 前端应用

前端项目目录，技术栈待定（纯 Web / 桌面应用 / 移动端）。

## 待定事项

- [ ] 确定技术栈
- [ ] 初始化工程

## 数据对接

爬虫脚本生成的数据存放在 `data/` 目录，图片资源在 `data/public/`。

前端可将 `data/public/` 映射为静态资源目录，JSON 中的图片路径已统一为 `/public/...` 格式，可直接引用。
